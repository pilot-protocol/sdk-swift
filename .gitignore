# Build artifacts — regenerated by scripts/build-xcframework.sh.
# The .xcframework is ~36 MB (3 slices × 12 MB) and should not be
# committed to the repository.
Frameworks/Pilot.xcframework/

# SwiftPM build output
.build/
.swiftpm/
Package.resolved
